#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    int sum = 0;
    cin >> N;

    for (int i = int(log10(N)+1); i > 0; i--) {
        bitset<i> bit;
    }
    cout << sum << endl;
}
